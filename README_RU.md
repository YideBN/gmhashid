⁉ Сменить язык на [Английский](README.md)
# 🎫 Простой HashID Модуль & Аддоны

Модуль добавляет клиентскую lua функцию GetHashID(), которая возвращает HWID компьютера, зашифрованный с помощью SHA256.

# Аддоны
## ⚙ HashIDLite
Просто проверяет, установлен ли у игрока модуль. Является основой для остальных аддонов.  
Добавляет такие команды, как:
-   gethashid nickname — выдаёт HashID игрока по его нику. Требует прав админа.
-   gethashidall — выдаёт HashID всех игроков сервера. Требует прав админа.

## 🎟 HashIDLog
Проверяет, установлен ли у игрока модуль, и закрепляет за его HashID его же SteamID64. Если игрок как-то сменит SteamID64, но HashID будет тот же, тогда сервер его не пустит. Данные о привязанных HashID хранятся в garrysmod/data/hashidloglist.txt.  
Добавляет такие команды, как:
-   hashidrm hashid — удаляет HashID из таблицы "logged" и из hashidloglist.txt. Требует прав админа.
-   hashidwrite — перезаписывает hashidloglist.txt. Требует прав суперадмина.
-   hashidreload — перезагружает таблицу "logged". Требует прав суперадмина.

## 🚫 HashIDBan
Проверяет, установлен ли у игрока модуль, и добавляет простую систему банов по HashID. Если игрок как-то сменит SteamID64, но HashID будет тот же, тогда сервер его не пустит, если он находится в бане по HashID. Данные о банах по HashID хранятся в garrysmod/data/hashidbanlist.txt.  
Добавляет такие команды, как:
-   banhashid nickname — банит игрока по его HashID с указанием причины и срока. Требует прав админа.
-   unbanhashid hashid — разбанивает игрока по его HashID. Требует прав админа.
-   hashidwrite — перезаписывает hashidbanlist.txt. Требует прав суперадмина.
-   hashidreload — перезагружает таблицу "banned". Требует прав суперадмина.

# 🔧 Установка
## 📻 Клиент
HashID модуль можно собрать из исходного кода или загрузить из раздела релизов. Его следует скачивать клиенту, если он хочет соединиться с сервером корректно. Устанавливается он в папку garrysmod/lua/bin.

## 🗃 Сервер
HashID аддоны можно загрузить из раздела релизов и редактировать их код. Их следует скачивать серверу для работы с HashID. Устанавливаются они в папку garrysmod/addons.

# 📟 Сборка(Для Windows)
**CompileHelpers не установят всё необходимое для сборки!**  
Вы должны установить всё, что требуется!  
Для сборки вам понадобятся:
-   WSL
-   CMake (и в терминале Windows, и в WSL Bash)
-   Ninja
-   и т.д.
